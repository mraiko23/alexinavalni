# Plants vs Brainrots Stock Tracker - Environment Variables
# Copy this file to .env and configure as needed

# Server Configuration
PORT=3000

# Proxy Configuration (optional)
# Uncomment and configure if external APIs are blocked in your region

# HTTP Proxy
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# Proxy with authentication
# HTTP_PROXY=http://username:password@proxy.example.com:8080
# HTTPS_PROXY=http://username:password@proxy.example.com:8080

# SOCKS5 Proxy (requires socks-proxy-agent package)
# HTTP_PROXY=socks5://proxy.example.com:1080
# HTTPS_PROXY=socks5://proxy.example.com:1080

# Polling Configuration (optional, in milliseconds)
# POLL_INTERVAL=10000  # Default: 10 seconds
# REQUEST_TIMEOUT=15000  # Default: 15 seconds

# Node Environment
NODE_ENV=production
